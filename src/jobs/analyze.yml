parameters:
  disabled:
    type: boolean
    default: false
  command:
    type: string
    default: yarn analyze

executor: default

steps:
  - unless:
      condition: <<parameters.disabled>>
      steps:
        - attach_workspace:
            at: .
        - run:
            command: <<parameters.command>>
